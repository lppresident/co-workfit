# AI Development Rules for Co-WorkFit

이 파일은 AI Assistant가 Co-WorkFit 프로젝트에서 개발할 때 **반드시 따라야 할 규칙**입니다.

## 🎯 핵심 원칙

### 1. **문서를 먼저 확인하라**
코딩하기 전에 **반드시** 다음 문서들을 확인하세요:

1. **UI_STRUCTURE.md** ← 페이지 만들 때 가장 먼저 확인
2. **AI_QUICK_REFERENCE.md** ← 빠른 템플릿 필요할 때
3. **CONTRIBUTING.md** ← 전체 기여 가이드
4. **ARCHITECTURE.md** ← Clean Architecture 패턴

### 2. **기존 코드를 참고하라**
새로운 기능을 만들 때:
- 비슷한 기능이 있는지 먼저 찾기
- 그 구조를 그대로 따르기
- 예제: `lib/features/social/` (친구, 리더보드 기능 참고)

### 3. **일관성이 최우선**
- 새로운 패턴을 만들지 말 것
- 기존 패턴을 따를 것
- 의심스러우면 문서 확인

## 📐 UI 구조 규칙 (필수!)

### ✅ MUST DO (반드시 해야 함)

#### 1. 모든 페이지는 BasePage 사용
```dart
// ✅ 올바름
class MyPage extends BasePage {
  const MyPage({super.key});
  @override
  State<MyPage> createState() => _MyPageState();
}

class _MyPageState extends BasePageState<MyPage> {
  @override
  void loadInitialData() {
    context.read<MyBloc>().add(LoadEvent());
  }

  @override
  PreferredSizeWidget buildAppBar(BuildContext context) {
    return StandardAppBar(title: 'My Page');
  }

  @override
  Widget buildBody(BuildContext context) {
    return BlocBuilder<MyBloc, MyState>(...);
  }
}

// ❌ 틀림 - 직접 StatefulWidget 사용 금지
class MyPage extends StatefulWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(...); // Wrong!
  }
}
```

#### 2. StandardAppBar 사용
```dart
// ✅ 올바름
StandardAppBar(
  title: '페이지 제목',
  actions: [
    AppBarActions.refresh(() => _refresh()),
    AppBarActions.filter(() => _filter()),
  ],
)

// ❌ 틀림 - 직접 AppBar 만들기 금지
AppBar(
  title: const Text('페이지 제목'),
  actions: [IconButton(...)], // Wrong!
)
```

#### 3. loadInitialData() 사용
```dart
// ✅ 올바름 - loadInitialData()에서 초기화
@override
void loadInitialData() {
  context.read<MyBloc>().add(LoadEvent());
}

// ❌ 틀림 - initState에서 BLoC 호출 금지
@override
void initState() {
  super.initState();
  context.read<MyBloc>().add(LoadEvent()); // Wrong! 위험함
}
```

#### 4. Mixin 사용

**탭이 있는 페이지:**
```dart
// ✅ 올바름
class _MyPageState extends BasePageState<MyPage>
    with SingleTickerProviderStateMixin, TabbedMixin {

  @override
  int get tabCount => 3;

  @override
  List<String> get tabLabels => ['탭1', '탭2', '탭3'];

  @override
  List<Widget> get tabViews => [Tab1(), Tab2(), Tab3()];
}

// ❌ 틀림 - 수동 TabController 관리 금지
late TabController _tabController;
@override
void initState() {
  _tabController = TabController(...); // Wrong!
}
```

**Pull-to-refresh가 필요한 페이지:**
```dart
// ✅ 올바름
class _MyPageState extends BasePageState<MyPage> with RefreshableMixin {
  @override
  Future<void> onRefresh() async {
    context.read<MyBloc>().add(RefreshEvent());
  }

  @override
  Widget buildBody(BuildContext context) {
    return buildRefreshableContent(child: ListView(...));
  }
}

// ❌ 틀림 - 직접 RefreshIndicator 만들기
RefreshIndicator(
  onRefresh: () async { ... }, // Wrong! Mixin 사용할 것
  child: ...,
)
```

#### 5. Common Widgets 사용
```dart
// ✅ 올바름
if (state is Loading) {
  return const CommonLoadingWidget(message: 'Loading...');
}

if (state is Error) {
  return CommonErrorWidget(
    message: state.message,
    onRetry: () => loadInitialData(),
  );
}

if (state is Loaded && state.items.isEmpty) {
  return const CommonEmptyWidget(
    icon: Icons.inbox,
    message: 'No data',
  );
}

// ❌ 틀림 - 커스텀 UI 만들기 금지
Center(
  child: Column(
    children: [
      const Icon(Icons.error),
      Text(error.message),
      ElevatedButton(...),
    ],
  ),
) // Wrong! CommonErrorWidget 사용할 것
```

#### 6. AppLogger 사용 (print 금지)
```dart
// ✅ 올바름
AppLogger.info('MyBloc', 'Loading data');
AppLogger.error('MyBloc', 'Failed to load', error);
AppLogger.debug('MyBloc', 'Debug info');

// ❌ 틀림
print('Loading data'); // 절대 사용 금지!
debugPrint('Loading data'); // 사용 금지!
```

#### 7. AppConstants 사용 (매직 넘버 금지)
```dart
// ✅ 올바름
padding: EdgeInsets.all(AppConstants.defaultPadding)
Color(AppConstants.goldColor)

// ❌ 틀림
padding: EdgeInsets.all(16.0) // Wrong!
Color(0xFFFFD700) // Wrong!
```

## 🏗️ Clean Architecture 규칙

### Layer 순서 (반드시 지킬 것)

1. **Domain Layer** (먼저 작성)
   - `domain/entities/` - Entity 정의
   - `domain/repositories/` - Repository 인터페이스
   - `domain/usecases/` - UseCase 작성

2. **Data Layer** (두 번째)
   - `data/models/` - Model (fromJson, toJson)
   - `data/datasources/` - DataSource (Firestore, API 등)
   - `data/repositories/` - Repository 구현체

3. **Presentation Layer** (마지막)
   - `presentation/bloc/` - Event, State, BLoC
   - `presentation/pages/` - BasePage 사용
   - `presentation/widgets/` - 재사용 위젯

4. **Dependency Injection** (통합)
   - `core/di/injection.dart` - 모든 의존성 등록
   - `main.dart` - BlocProvider 추가

### ❌ 절대 하지 말 것

1. **Domain이 Data나 Presentation에 의존** - 절대 금지!
2. **UI에서 직접 DataSource 호출** - 반드시 BLoC 거칠 것
3. **Repository 건너뛰기** - 항상 Repository 통해 데이터 접근
4. **UseCase 없이 Repository 직접 호출** - BLoC에서 UseCase 사용

## 📝 Naming Conventions (절대 규칙)

### Files
```
entity → {name}_entity.dart
model → {name}_model.dart
repository → {name}_repository.dart (interface)
repository impl → {name}_repository_impl.dart
usecase → {action}_{object}.dart (get_users.dart)
datasource → {source}_{name}_datasource.dart
bloc → {name}_bloc.dart, {name}_event.dart, {name}_state.dart
page → {name}_page.dart
widget → {name}_widget.dart or {name}_tab.dart
```

### Classes
```
Entity → UserEntity
Model → UserModel (extends UserEntity)
Repository → UserRepository
UseCase → GetUser, CreateUser, UpdateUser
Event → LoadUsers, CreateUser
State → UserLoading, UserLoaded, UserError
BLoC → UserBloc
Page → UserPage extends BasePage
```

## 🔍 체크리스트 (코딩 전 확인)

새 기능 추가 시 **반드시** 이 순서대로:

- [ ] 1. 문서 확인 (UI_STRUCTURE.md, AI_QUICK_REFERENCE.md)
- [ ] 2. 비슷한 기능 찾기 (lib/features/social/ 등)
- [ ] 3. Domain Layer 작성 (Entity → Repository Interface → UseCases)
- [ ] 4. Data Layer 작성 (Model → DataSource → Repository Impl)
- [ ] 5. Presentation Layer 작성 (Event → State → BLoC)
- [ ] 6. UI 작성 (**BasePage 필수**, Mixins 활용)
- [ ] 7. DI 설정 (injection.dart → main.dart)
- [ ] 8. `flutter analyze` 실행 (0 errors 확인)
- [ ] 9. Common widgets 사용 확인
- [ ] 10. AppLogger 사용 확인 (print 없는지 확인)

## ⚡ 빠른 참조

### 페이지 타입별 템플릿

| 필요한 기능 | Base | Mixins | 참고 파일 |
|------------|------|--------|----------|
| 단순 페이지 | `BasePage` | - | `UI_STRUCTURE.md` Simple Page |
| 탭 페이지 | `BasePage` | `TabbedMixin` | `friends_page.dart` (4탭) |
| 새로고침 목록 | `BasePage` | `RefreshableMixin` | `UI_STRUCTURE.md` Refreshable |
| 상태 관리 | `BasePage` | `LoadableMixin` | `UI_STRUCTURE.md` Pattern 3 |

### 자주 사용하는 import

```dart
// Base & Mixins
import 'package:co_workfit/core/presentation/base_page.dart';
import 'package:co_workfit/core/presentation/mixins/tabbed_mixin.dart';
import 'package:co_workfit/core/presentation/mixins/refreshable_mixin.dart';
import 'package:co_workfit/core/presentation/mixins/loadable_mixin.dart';

// Common Widgets
import 'package:co_workfit/core/widgets/common_loading_widget.dart';
import 'package:co_workfit/core/widgets/common_error_widget.dart';
import 'package:co_workfit/core/widgets/common_empty_widget.dart';

// UI Components
import 'package:co_workfit/core/presentation/widgets/standard_app_bar.dart';

// Utils
import 'package:co_workfit/core/utils/logger.dart';
import 'package:co_workfit/core/constants/app_constants.dart';
```

## 🚨 자주 하는 실수

### 1. initState에서 BLoC 호출
```dart
// ❌ WRONG
@override
void initState() {
  super.initState();
  context.read<MyBloc>().add(LoadEvent()); // BuildContext 위험!
}

// ✅ CORRECT
@override
void loadInitialData() {
  context.read<MyBloc>().add(LoadEvent()); // 안전함
}
```

### 2. Scaffold 직접 만들기
```dart
// ❌ WRONG
class MyPage extends StatefulWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(...),
      body: ...,
    );
  }
}

// ✅ CORRECT
class MyPage extends BasePage {
  @override
  State<MyPage> createState() => _MyPageState();
}

class _MyPageState extends BasePageState<MyPage> {
  @override
  PreferredSizeWidget buildAppBar(BuildContext context) {
    return StandardAppBar(...);
  }

  @override
  Widget buildBody(BuildContext context) {
    return ...;
  }
}
```

### 3. TabController 수동 관리
```dart
// ❌ WRONG
late TabController _tabController;

@override
void initState() {
  super.initState();
  _tabController = TabController(length: 3, vsync: this);
}

@override
void dispose() {
  _tabController.dispose();
  super.dispose();
}

// ✅ CORRECT
class _MyPageState extends BasePageState<MyPage>
    with SingleTickerProviderStateMixin, TabbedMixin {

  @override
  int get tabCount => 3;

  @override
  List<String> get tabLabels => ['탭1', '탭2', '탭3'];
  // TabController는 자동 관리됨!
}
```

## 📚 학습 순서 (처음 시작하는 AI)

1. **UI_STRUCTURE.md** 읽기 (30분) - 가장 중요!
2. **AI_QUICK_REFERENCE.md** 읽기 (15분) - 템플릿 익히기
3. **friends_page.dart** 코드 읽기 (10분) - 실제 예제
4. **leaderboard_page.dart** 코드 읽기 (10분) - 또 다른 예제
5. **CONTRIBUTING.md** 읽기 (20분) - 전체 프로세스
6. **ARCHITECTURE.md** 읽기 (20분) - Clean Architecture

총 소요 시간: 약 1.5시간 (하지만 이후 개발 속도 3배 향상!)

## ⚖️ 우선순위

코드 작성 시 우선순위:

1. **일관성** (Consistency) - 가장 중요!
2. **문서 준수** (Follow Docs) - 문서가 법
3. **기존 패턴** (Existing Patterns) - 새로운 것 만들지 말 것
4. **간결성** (Simplicity) - 복잡하게 만들지 말 것
5. **성능** (Performance) - 일관성이 우선

## 🎓 Remember

> "완벽한 코드보다 일관된 코드가 낫다"
> "새로운 패턴보다 기존 패턴이 낫다"
> "영리한 코드보다 명확한 코드가 낫다"

---

**이 규칙을 어기면 코드 리뷰에서 거부됩니다!**

문서를 읽고, 예제를 보고, 패턴을 따르세요. 그러면 완벽한 코드를 작성할 수 있습니다.
